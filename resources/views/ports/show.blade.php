@extends('layouts.app') @section('title',$port->name) @section('content')<div class="container-fluid"><div class="d-flex justify-content-between mb-4"><div><h3>{{ $port->name }}</h3><p class="text-muted">{{ $port->country?->name }}</p></div><a href="{{ route('ports.index') }}" class="btn btn-outline-secondary">Kembali</a></div><div class="info-card"><div class="card-body p-0"><div id="portMap" style="height:500px"></div></div></div></div>@endsection @push('styles')<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css">@endpush @push('scripts')<script src="https://unpkg.com/leaflet@1.9.4/dist/leaflet.js"></script><script>const map=L.map('portMap').setView([{{ (float)$port->latitude }},{{ (float)$port->longitude }}],10);L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'&copy; OpenStreetMap'}).addTo(map);L.marker([{{ (float)$port->latitude }},{{ (float)$port->longitude }}]).addTo(map).bindPopup(@json($port->name)).openPopup();</script>@endpush
