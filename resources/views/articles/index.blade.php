@extends('layouts.app') @section('title','Artikel Analisis') @section('content')<div class="container-fluid"><div class="d-flex justify-content-between mb-4"><div><h3>Artikel Analisis</h3><p class="text-muted">Kelola insight dan laporan supply chain.</p></div><a href="{{ route('admin.articles.create') }}" class="btn btn-primary">Tambah Artikel</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="info-card"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th class="ps-4">Judul</th><th>Status</th><th>Penulis</th><th>Publikasi</th><th>Aksi</th></tr></thead><tbody>@forelse($articles as $article)<tr><td class="ps-4 fw-semibold">{{ $article->title }}</td><td><span class="badge text-bg-{{ $article->status==='Published'?'success':'secondary' }}">{{ $article->status }}</span></td><td>{{ $article->user?->name }}</td><td>{{ $article->published_at?->format('d M Y') ?? '-' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('admin.articles.show',$article) }}"><i class="bi bi-eye"></i></a> <a class="btn btn-sm btn-outline-secondary" href="{{ route('admin.articles.edit',$article) }}"><i class="bi bi-pencil"></i></a> <form class="d-inline" method="POST" action="{{ route('admin.articles.destroy',$article) }}" onsubmit="return confirm('Hapus artikel?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-4">Belum ada artikel.</td></tr>@endforelse</tbody></table></div><div class="card-body">{{ $articles->links() }}</div></div></div>@endsection
