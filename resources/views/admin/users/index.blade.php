@extends('layouts.app') @section('title','Kelola Pengguna') @section('content')<div class="container-fluid"><div class="mb-4"><h3>Kelola Pengguna</h3><p class="text-muted">Atur peran dan akun pengguna platform.</p></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="info-card"><div class="card-body p-0"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead class="table-light"><tr><th class="ps-4">Nama</th><th>Email</th><th>Peran</th><th>Terdaftar</th><th>Aksi</th></tr></thead><tbody>@foreach($users as $user)<tr><td class="ps-4 fw-semibold">{{ $user->name }}</td><td>{{ $user->email }}</td><td><form method="POST" action="{{ route('admin.users.role',$user) }}" class="d-flex gap-2">@csrf @method('PATCH')<select name="peran" class="form-select form-select-sm" style="width:120px" {{ $user->is(auth()->user()) ? 'disabled' : '' }}><option value="user" @selected($user->peran==='user')>User</option><option value="admin" @selected($user->peran==='admin')>Admin</option></select>@unless($user->is(auth()->user()))<button class="btn btn-sm btn-outline-primary">Simpan</button>@endunless</form></td><td>{{ $user->created_at->format('d M Y') }}</td><td>@unless($user->is(auth()->user()))<form method="POST" action="{{ route('admin.users.destroy',$user) }}" onsubmit="return confirm('Hapus pengguna ini?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></button></form>@else<span class="badge text-bg-secondary">Akun Anda</span>@endunless</td></tr>@endforeach</tbody></table></div></div><div class="card-body border-top">{{ $users->links() }}</div></div></div>@endsection
